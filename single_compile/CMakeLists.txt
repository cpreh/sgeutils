sge_utils_add_program(
	single_compile
	SOURCES
		single_compile.cpp
	LINK_LIBS
		${fcppt_core_TARGET}
		${sgecore_TARGET}
		${sgeparsejson_TARGET}
		${Boost_FILESYSTEM_LIBRARY}
		${Boost_PROGRAM_OPTIONS_LIBRARY}
		${Boost_REGEX_LIBRARY}
)
